---
- name: Generate .env configuration file from template
  ansible.builtin.template:
    src: .env.j2
    dest: "{{ project_directory }}/.env"
    owner: ubuntu
    group: ubuntu
    mode: '0600'
  notify: Restart Docker services
...
